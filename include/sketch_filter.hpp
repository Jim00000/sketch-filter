#ifndef _SKETCH_FILTER_HPP_
#define _SKETCH_FILTER_HPP_

namespace sketch
{
    class sketch_filter
    {
    public:
        sketch_filter();
        ~sketch_filter();

    protected:

    private:

    };
}

#endif